management:
  endpoints:
    web:
      base-path: "/"
      path-mapping:
        prometheus: metrics
      exposure:
        include: health
  endpoint:
    metrics:
      enabled: true
    prometheus:
      enabled: false