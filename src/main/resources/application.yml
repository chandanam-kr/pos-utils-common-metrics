management:
  endpoints:
    web:
      base-path: "/actuator"
      exposure:
        include: health
  endpoint:
    prometheus:
      enabled: false      # IMPORTANT: Disable to use our /metrics endpoint
    health:
      enabled: true

kind: Application